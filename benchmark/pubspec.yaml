name: jserializer_benchmark
description: Benchmark comparing JSerializer vs vanilla manual JSON serialization
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  jserializer:
    path: ../jserializer
